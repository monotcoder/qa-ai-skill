# 发版须知

写给维护者，学员不用看。这个文件不在 `skills/` 下，不会被装到学员电脑上。

## 兼容铁律

1. **每个 skill 正文里的 `<!-- qa-ai-skill id=xxx -->` 永远不改，id 永不复用。**
   更新靠它认人，不靠目录名。
2. **目录名可以随便改** —— id 不变，更新就认得出来。
3. **删掉一个 skill，必须写进 `版本.json` 的 `已移除`，写清原因。**
   不写的话，学员本地会永远留着一个过时的，而且还会被触发。
4. **学员项目里的目录和字段：只加，不改名，不删。**
   新加的东西必须可以没有 —— 老项目缺它时，skill 照旧能跑，最后提一句可以补齐。
5. **做不到第 4 条，第一位版本号 +1**，更新说明里写清"你需要做什么"。

## 版本号

`大.小.修`

| | 什么时候 | 学员要做什么 |
| --- | --- | --- |
| 大 | 学员可能要动项目 | 看更新说明，按说明做 |
| 小 | 加了功能 | 无 |
| 修 | 改错字、改措辞 | 无 |

## 发版步骤

1. 改 `skills/qa-更新/版本.json` 的版本号；skill 有增删，同步改列表
2. `更新说明.md` 顶上加一条，**必须写"你需要做什么"**
3. 提交
4. **打 tag `v<版本号>`** —— `/qa-更新` 靠它判断学员改没改过 skill，
   漏打一次，那一版的所有学员都会被当成"改过"，每个 skill 都要被问一遍
5. 推送，**带上 tag**
