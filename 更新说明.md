# 更新说明

每个版本写三件事：多了什么、改了什么、**你需要做什么**。

`/qa-更新` 会读这个文件，只把你还没装过的那几版讲给你听。

## 1.2.0 · 2026-09-23

用户提的两个问题：没写过用例，凭什么信「通过」；想回归某个流程，该怎么说。

**多了什么**

- `/qa-3-出用例`：**AI 起草用例，你审**。两种入口都支持：
  **开发阶段**有 PRD，照 PRD 先写用例（标「待实现」，跑的时候自动跳过）；
  **测试阶段**没有 PRD 或者 PRD 跟页面对不上，就走一遍页面再起草
- 没有 PRD 时，预期只从四个地方来：产品自己的说法（文案、提示）、不依赖需求的规律
  （不崩、数字自洽、看不到别人的数据）、两个端互相验证、上一版的行为。
  **"系统现在就是这样"不算预期** —— 那是拿系统证明系统
- PRD 跟成品对不上：照 PRD 写，标「冲突待裁决」，跑出来判「说不准」，你来裁决。
  **不准自己选一边**，默认"以成品为准"等于永远不报 bug
- 你确认过的猜测会记上日期存进用例库。攒下来，**这就是项目事后补出来的需求文档**
- **回归可以点名了**：「跑批次发布」「跑 B-02 到 B-04」「全部跑一遍」「跑冒烟」。
  项目里多一份 `cases/用例清单.md`，一页纸列出有哪些流程、哪些编号
- **每条都能回放**：跑的时候录下过程，不信哪条，点开一步步看它点了什么

**改了什么**

- 重新编号：跑测试 3→4、存成脚本 4→5、提bug 5→6
- **AI 猜的预期不准判「通过」**，最高只能判「说不准」，等你确认。
  不然就是拿系统现在的样子证明系统是对的，bug 会被原样判成通过
- 每条用例都要标 `预期来源`：PRD / 产品说明 / AI推测 / 用户确认
- **报告第一段先写「这轮没验什么」**，不写在结尾。只跑了冒烟就直说"功能一个都没测"

**你需要做什么**

- 打 `/qa-更新`
- 以前打 `/qa-3-跑测试` 的，现在打 `/qa-4-跑测试`；存成脚本和提bug 也各往后挪了一位
- 想让 AI 起草用例：`/qa-3-出用例`，有 PRD 就一起给它

## 1.1.0 · 2026-09-22

第一次拿真实项目跑下来，把踩到的坑改回了 skill 里。

**多了什么**

- `/qa-4-存成脚本`：把跑通过的存成脚本。以后每次发版跑脚本就行，几十秒，结果是确定的
- `项目适配.md`：你项目自己的做法（工具、登录、等待、能不能并行）记在这里。skill 只管"查什么"，"怎么做"按你的项目来。升级永远不碰这个文件
- 每个命令开始前会告诉你能不能看、怎么看；做的过程中边做边说；**做完就停，等你点头**
- 每个命令收尾时会写明「项目适配.md：补了 N 条 / 这次没补」，写没写你一眼看得到

**改了什么**

- `/qa-4-提bug` 改名为 **`/qa-5-提bug`**，功能不变。流程变成：跑测试 → 通过的存成脚本 → 没通过的、你确认过的提 bug
- 下结论之前先核实一次：数字对不上先看口径；界面空了先看接口有没有数据；时红时绿先查并发
- 按名字找元素改成精确匹配（「登录」不会再误中「游客登录」）
- 不再收集别人的个人信息来证明 bug
- 建目录后会自己先跑一次登录，跑通了才算建完；要密码的让你自己登录，密码不进任何文件
- 不再替你换掉项目里已经在用的测试工具

**你需要做什么**

- 打 `/qa-更新`
- **老项目需要补齐**：在项目里跑一次 `/qa-1-建目录`，它会补上 `项目适配.md`，
  并在项目的 AI 说明文件（`CLAUDE.md` / `AGENTS.md`）末尾加一句提示。只补缺的，已有的不动
- 不补也能用：2、3、4 号第一次跑时发现没有 `项目适配.md`，会当场建一个
- 以前打 `/qa-4-提bug` 的，现在打 `/qa-5-提bug`

## 1.0.0 · 2026-09-21

首个版本：建目录、走一遍页面、跑测试、提 bug、更新。

- **你需要做什么**：照 README 装一次。以后更新打 `/qa-更新` 就行
