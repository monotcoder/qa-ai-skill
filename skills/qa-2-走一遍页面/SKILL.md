---
name: qa-2-走一遍页面
description: 自己把待测站点的页面点一遍，记下有哪些页面、彼此怎么跳，写进 maps/。用户说"走一遍页面""看看这站有哪些页面""画页面地图"时使用。这一步只记录，不判断对错。
---

<!-- qa-ai-skill id=explore -->

# 第 2 步 · 自己走一遍，记下有哪些页面

**这一步不判断任何东西对不对。** 只记录。看到明显是 bug 的东西，写进 `notes`，继续走。

全程零点击 —— 用户不碰鼠标。

## 开始前

1. 读项目根目录的 `项目适配.md`，这个项目的做法和踩过的坑都在里面。
   **没有就当场建一个**：按 `qa-1-建目录` 那个 skill 目录里的 `项目适配模板.md`，
   只建这一个文件，别的不动，建完告诉用户。第一次跑踩的坑最多，没地方写就全丢了。
2. 读 `prompts/02-no-touch.md`。**没有这个文件就停下来问**，不要凭感觉判断什么能点。
3. 确认登录态还有效（`seeds/.auth/` 下对应角色的那份）。
   **同一个浏览器里，一个站点只能登一个账号**。要换账号，就换一个互相隔离的浏览器环境，不要在同一个里退出再登。
4. **告诉用户能不能看、怎么看**。你用的浏览器他看得见吗？看不见就直说，并告诉他打开的办法。
   不要让他以为你在操作浏览器，却什么都看不到。

## 边走边说

每走完一个页面，报一句：走到哪了、看到了什么、有没有记下可疑的。
**不要闷头走完 60 步才开口**——用户不知道你在干什么，就没法叫停。

## 硬约束

1. **读无障碍树，不要截图。** 每屏只取可交互元素和可见文本，丢掉布局容器。
   同一屏：截图约 1500~3000 token，aria 树约 200~600 —— 差 10 倍，而且便宜的那个更准。

2. **命中禁点清单的元素：记录它存在，不要点。**

3. **限深限步**：最多 60 步、最深 5 层。遇到记过的页面就回退。

4. **不要自己造数据。** 空列表页如实记成 `空状态，没走进去`，
   不要去注册、下单、上传把它填满 —— 数据是 `seeds/` 的事。

5. **要输入时只用 `seeds/fixtures.json` 里的值**，不许自己编。

6. **有多个系统时，一个系统一个系统地走**，写进 `maps/<系统>/`，
   各自对照 `02-no-touch.md` 里该系统那一段。不要从一个系统顺着链接走进另一个系统。

7. **`notes` 里记的怀疑，先换一种方式核实一次。** 读取工具的显示可能不全：
   比如它说"链接没有名字"，去看一眼元素里的文字；说"数字对不上"，先看清标签是不是同一个口径。
   核实不了就写成「怀疑」，并写上怎么核实的。

8. **不汇总别人的个人信息。** 页面上看到别人的 IP、手机号、邮箱：记下"这个页面显示了 XX"，
   **不要去收集、统计、导出这些值**，哪怕是为了证明这是个 bug。

## 输出 `maps/<模块>.yaml`

```yaml
模块: 优惠券
时间: 2026-09-20T14:30
起点: /dashboard

页面:
  个人中心:
    路径: /profile
    可点的:
      - { 名称: 卡券包,   类型: link }
      - { 名称: 注销账户, 类型: button, 状态: 禁点清单命中·没点 }
    跳转:
      卡券包: 优惠券列表
    notes: []

没走到的:
  - 页面: 优惠券详情
    原因: 要点「立即使用」，会进下单流程，超出本模块范围
```

## 必须停下来报告

- 出现禁点清单之外、但明显危险的操作（付款、发送、注销、删除全部）
- 连续 3 步没发现新页面
- 跳到登录页 —— **别去手动登录**，说明登录态失效了，停下来说
- 页面崩溃或白屏 —— 这是发现，写进 `notes` 继续走

## ⚠️ 走不通必须上报，不准自己换路

计划走 A，A 走不通时：

```yaml
走不通:
  - 计划: 从「个人中心」点「卡券包」
    实际: 「卡券包」不存在
    看到的: [订单, 设置, 帮助]
```

**静默绕过是这套流程里最危险的行为。走不通本身就是信息。**

## 收尾前：检查要不要写进 `项目适配.md`

逐条过一遍。**有任何一条，就必须写进「踩过的坑」**（做法 / 为什么 / 日期）：

- 换过等待方式（比如这个站不能等网络完全安静）
- 调整过并发（比如只能一个浏览器跑）
- 登录方式跟记录的不一样，或者多用了一个账号
- 为了跑通，改过配置文件
- 撤回过误报，而误报的原因是这个项目特有的
- 某种元素只能用特殊的方式找到

收尾汇报里**必须有一行**：`项目适配.md：补了 N 条（逐条列出）` 或 `项目适配.md：这次没补`。
不能只靠自觉——写没写，要让用户一眼看得到。

## 做完就停

告诉用户：走了几个页面、写进了哪个文件、`notes` 里记了几条怀疑、哪些没走到以及为什么。
然后**停下**。下一步是 `/qa-3-跑测试`，由用户决定什么时候开始。
**不要自己开始判对错、写问题清单**——那是第 3 步的事。

## 老项目缺文件时

项目是旧版建的、缺了这一步要读的某个文件：**按没有它的方式照常做，不要报错停下**。
做完最后提一句：「新版多了 XX，可以跑一次 `/qa-1-建目录` 补上」。
